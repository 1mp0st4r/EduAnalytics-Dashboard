action_endpoint:
  url: "http://localhost:5055/webhook"

tracker_store:
  type: SQL
  dialect: "postgresql"
  url: "postgresql://username:password@localhost/rasa"
  login_db: "postgres"

event_broker:
  type: SQL
  dialect: "postgresql"
  url: "postgresql://username:password@localhost/rasa"
  login_db: "postgres"
